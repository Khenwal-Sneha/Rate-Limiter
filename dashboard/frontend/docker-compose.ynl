version: "3.8"

services:

  frontend:
    build: .
    ports:
      - "5173:80"
    depends_on:
      - backend

  backend:
    image: your-backend-image   # or build your Spring Boot here
    ports:
      - "8080:8080"